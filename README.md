# Strap Kit JS
```sh
$ grunt clean
$ grunt copy
$ grunt addFeatures --features=[folder/to/features] # Can use "addFeatures --features=./features" to test
$ grunt browserify # this only works for android right now
```
